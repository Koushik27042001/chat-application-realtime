# Vite dev server proxies /api to this backend URL.
# If port 5000 is used by another app, change backend PORT in backend/.env
# and set the matching URL here, e.g. VITE_BACKEND_PROXY=http://localhost:5001

VITE_BACKEND_PROXY=http://localhost:5000
